<Page x:Class="WpfApp2.Views.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AboutPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Album Details" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="1">
            <Grid DataContext="{Binding SelectedAlbum}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Grid.Column="0" Text="User ID:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding userId}" Margin="10,5"/>
                
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Album ID:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding id}" Margin="10,5"/>
                
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Title:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding title}" Margin="10,5" TextWrapping="Wrap"/>
                
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Description:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding describtion}" Margin="10,5" TextWrapping="Wrap"/>
            </Grid>
            
            <!-- Loading indicator -->
            <TextBlock Text="Loading..." HorizontalAlignment="Center" 
                       Margin="0,20,0,0" FontStyle="Italic" FontSize="16"/>
        </StackPanel>
        
        <Button Grid.Row="2" Content="Back to Albums" HorizontalAlignment="Center" 
                Margin="0,20,0,0" Padding="10,5" Click="BackButton_Click"/>
    </Grid>
</Page>
