<Page x:Class="WpfApp2.Views.AlbumsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AlbumsPage">

    <Grid>
        <DataGrid ItemsSource="{Binding Albums}"
     AutoGenerateColumns="False"
     Margin="10"
     IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="User ID">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Hyperlink Click="UserID_Click" Tag="{Binding userId}">
                                    <TextBlock Text="{Binding userId}"/>
                                </Hyperlink>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Album ID" Binding="{Binding id}" />
                <DataGridTextColumn Header="Title" Binding="{Binding title}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
